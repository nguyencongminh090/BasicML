---
description: Classify, branch-place, and commit the current changes
---

Use the `commit-task` skill to classify the current changes, place them on the right branch per this repo's branch policy (main protected; feat/fix each get their own branch off dev), then commit via the `git-commit` skill.
