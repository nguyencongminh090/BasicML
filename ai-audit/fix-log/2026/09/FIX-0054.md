---
id: FIX-0054
timestamp: 2026-09-13T05:46:10+07:00
todo_id: TODO-0033
---

## Prompt

"DO TODO-0033" — finish the `Loss`/`Optimizer` half of AdvanceML's Module API (split off TODO-0032 for scope). WIP-cap exception extended to a 4th in-progress slot (confirmed via AskUserQuestion).

## Action

- Added `AdvanceML/include/advanceml/optim/optimizer.hpp` + `src/optim/optimizer.cpp` — abstract `Optimizer` base (`step()`, `zero_grad()`).
- Moved `SGD` from the old flat `advanceml/optim.hpp`/`src/optim.cpp` into `AdvanceML/include/advanceml/optim/sgd.hpp` + `src/optim/sgd.cpp`, now deriving from `Optimizer` (`override` on both methods); behavior unchanged.
- Deleted the old `advanceml/optim.hpp`/`src/optim.cpp`.
- Added `AdvanceML/include/advanceml/nn/loss.hpp` + `src/nn/loss.cpp` — abstract `Loss` base (`operator()(pred, target) -> Tensor`) and `MSELoss` wrapping the existing `mse_loss` free function.
- Updated `AdvanceML/tests/autograd_test.cpp`'s include (`optim.hpp` → `optim/sgd.hpp`) and rewrote `AdvanceML/tests/nn_test.cpp`'s XOR test to use `MSELoss`/`SGD` (via the new headers) instead of the raw `mse_loss` free function, so it now exercises the complete `Sequential`/`Linear`/`ReLU`/`MSELoss`/`SGD` API end-to-end.
- Updated `AdvanceML/CMakeLists.txt` source list accordingly.

## Decision

`Loss` needs no separate `backward()` (unlike BasicML) since `operator()`'s returned `Tensor` already carries a `grad_fn` back to `pred` through whichever op the subclass calls (`mse_loss` here) — calling `.backward()` on that result is sufficient, matching the design noted in TODO-0032/0033's instructions.

## Conclusion

Fixed/implemented. `cmake --build AdvanceML/build` succeeds; `ctest --test-dir AdvanceML/build --output-on-failure` passes all 5 tests, including the pre-existing `SGD` test with unmodified behavior. This closes out TODO-0032's original full scope (Module/Linear/ReLU/Sequential + Loss/Optimizer), split across TODO-0032 and TODO-0033.
