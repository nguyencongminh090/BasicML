---
id: FIX-0031
timestamp: 2026-09-05T13:07:08+07:00
todo_id: TODO-0012
---

## Prompt

User (Vietnamese): remove the gray line from panel 5 of
`plot_dynamic_linear_2features.py` — it's a static reference curve, not the
training data, and adds no value.

## Action

- Removed the `ax_costw.plot(w1_curve, cost_curve, color="gray", ...)` call
  in `animate()` and its `cost_vs_w1()` inputs; panel 5 now only plots the
  purple `costw_line` optimizer trajectory.
- Reset `ax_costw.set_ylim` to scale from `history.cost.max()` instead of
  `max(cost_curve.max(), history.cost.max())` since `cost_curve` no longer
  exists.
- Deleted the now-unused `cost_vs_w1()` helper function entirely (was only
  called from the removed plot line).
- Re-ran the script headlessly (`MPLBACKEND=Agg`, `/usr/bin/python3.13`) and
  rendered the figure to a PNG to confirm panel 5 shows only the purple
  trajectory with no leftover gray curve or unused-reference artifacts.

## Decision

Deleted the helper rather than keeping it unused, per repo convention (no
dead code / unused abstractions).

## Conclusion

Fixed. Panel 5 now shows only the actual training trajectory; verified via
headless run + rendered PNG.
