---
id: FIX-0019
timestamp: 2026-09-03T01:21:53+07:00
todo_id: TODO-0009
---

## Prompt

"small modify: Use Adam for Logistic and Linear? (examples)". AskUserQuestion
answer: switch both.

## Action

On `feature/TODO-0009-more-optimizers`:

- `BasicML/examples/train_linear.py` — `from basicml.optim.momentum import
  Momentum` → `from basicml.optim.adam import Adam`; optimizer now
  `Adam(model.parameters(), lr=LEARN_RATE)`; `LEARN_RATE` 0.01 → 0.1, `EPOCHS`
  200 → 400; removed `MOMENTUM`; module + `train()` docstrings updated.
- `BasicML/examples/train_logistic.py` — same optimizer swap; `LEARN_RATE`
  0.07 → 0.1, `MAX_EPOCHS` 20_000 → 3_000, `LOG_EVERY` 2_000 → 250; removed
  `MOMENTUM`; module docstring updated.
- Re-aligned both import blocks to the column-aligned convention.

## Decision

The user asked directly; both examples already sit alongside the new
`train_optimizer_comparison.py`, so standardising the two single-model examples
on Adam keeps the example set coherent. Epoch budgets cut because Adam
converges far faster here (logistic went from a 20k budget to 538 real epochs).
`lr` retuned to ~0.1 — Adam's per-parameter RMS scaling tolerates the larger
step on this unnormalised (linear) / normalised (logistic) data.

## Conclusion

Fixed. `python3.13 BasicML/examples/train_linear.py` → `f(x) = 2.0000x +
-0.0000`. `MPLBACKEND=Agg python3.13 BasicML/examples/train_logistic.py` →
converges after 538 epochs, loss 0.04998. `pyrefly check` — 9 pre-existing
errors, none in the touched files.
