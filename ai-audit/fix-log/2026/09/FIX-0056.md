---
id: FIX-0056
timestamp: 2026-09-13T06:06:46+07:00
todo_id: TODO-0036
---

## Prompt

"DO task TODO-0034" (the AdvanceML component-library umbrella). Umbrella wasn't directly workable, so asked the user which sub-area and confirmed a WIP-cap exception; user picked area 4 (more optimizers) and approved the exception.

## Action

Added `Momentum`, `Adam`, `AdamW` optimizers to AdvanceML:
- `AdvanceML/include/advanceml/optim/{momentum,adam,adamw}.hpp` + matching `AdvanceML/src/optim/*.cpp`, each deriving from `advanceml::Optimizer`, operating on `Tensor::grad()`/`Tensor::zero_grad()` like the existing `SGD`.
- Registered the new sources in `AdvanceML/CMakeLists.txt`.
- Added `AdvanceML/tests/optim_test.cpp` (per-step closed-form checks for all three, plus an Adam XOR-MLP loss-decrease training test) and registered it in `AdvanceML/tests/CMakeLists.txt`.
- Built with `cmake --build AdvanceML/build` and ran `ctest --test-dir AdvanceML/build` — 13/13 tests pass (9 pre-existing + 4 new).

## Decision

Matched `basicml.optim.{momentum,adam,adamw}`'s update math (velocity/moment recurrences, bias correction, decoupled weight decay for AdamW) without porting the Python — same "conceptually the same shape, not a port" convention `SGD`'s header comment already establishes, since AdvanceML's `Tensor` has autograd-managed `.grad()`/`.zero_grad()` instead of BasicML's regularizer-aware `Optimizer` base.

## Conclusion

Fixed. All tests green. Filed as sub-TODO TODO-0036 under the TODO-0034 umbrella; TODO-0034 itself stays open (CNN layers, losses, datasets/metrics, MNIST example still outstanding).
