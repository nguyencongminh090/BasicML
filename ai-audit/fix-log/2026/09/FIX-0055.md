---
id: FIX-0055
timestamp: 2026-09-13T05:58:26+07:00
todo_id: TODO-0035
---

## Prompt

"DO task TODO-0034" → narrowed via AskUserQuestion to its first sub-TODO, TODO-0035 (more activation Modules), with a WIP-cap exception (3rd in-progress slot, TODO-0011/TODO-0024 already occupying the normal 2-slot cap) and the user choosing area 2 (activations) over the umbrella's suggested area 1 (CNN layers).

## Action

Committed the previously-pending TODO-0032/0033 work first (see below), merged `feature/TODO-0032-module-api` into `dev`, then branched `feature/TODO-0035-activations` off `dev`. Added:
- `sigmoid`, `leaky_relu`, `gelu`, `softmax` free-function ops in `AdvanceML/include/advanceml/ops.hpp` + `src/ops.cpp`, each building its own autograd `Node`/`backward_fn` like the existing `relu`/`mse_loss` ops. `gelu` uses the exact erf-based form; `softmax` operates over the last axis (whole vector for 1D, per-row for 2D) with max-subtraction for numerical stability.
- `Sigmoid`, `LeakyReLU`, `GELU`, `Softmax` `Module` wrappers under `AdvanceML/include/advanceml/nn/` + `src/nn/`, mirroring `ReLU`'s parameterless-wrapper shape (`LeakyReLU` takes a `negative_slope` constructor arg, default `0.01`).
- Wired the four new `nn/*.cpp` sources into `AdvanceML/CMakeLists.txt`.
- `AdvanceML/tests/nn_test.cpp`: one numerical-gradient-check test per new activation (shared `check_activation_gradient` helper feeding each through `mse_loss` and comparing analytic vs. central-difference gradients, same pattern as `autograd_test.cpp`), plus a row-sums-to-1 check for `Softmax`.

Also, as a prerequisite: committed the uncommitted TODO-0032 (`Module`/`Linear`/`ReLU`/`Sequential`) and TODO-0033 (`Loss`/`Optimizer` base classes) work that was sitting in the working tree from a prior session, split into a `feat` commit (AdvanceML code) and a `chore` commit (ai-audit bookkeeping: archiving TODO-0031/0032/0033, filing TODO-0034/0035), then merged `feature/TODO-0032-module-api` into `dev`.

## Decision

Kept `LeakyReLU`'s slope as a constructor parameter rather than a free function default baked into the `Module`, matching `Linear`'s pattern of layer-level configuration. Used the exact (erf) `GELU` rather than the tanh approximation since correctness against a numerical gradient check was prioritized over the extra `erf`/`exp` cost — this is a from-scratch, no-BLAS-hot-path activation, not on the CNN throughput-critical path. `Softmax` deliberately restricted to 1D/2D consistent with `Tensor`'s current milestone-1 rank support; no `dim` parameter yet since there's no rank-3+ tensor to disambiguate against.

## Conclusion

Fixed/implemented. `cmake --build AdvanceML/build` succeeds; `ctest --test-dir AdvanceML/build --output-on-failure` passes all 9 tests (5 pre-existing + 4 new). Not yet merged to `dev` or archived — TODO-0035 stays `in-progress` pending merge.
