---
id: FIX-0020
timestamp: 2026-09-03T01:34:25+07:00
todo_id: TODO-0009
---

## Prompt

"Update demo with Adam Optimizer". AskUserQuestion answers: move *all*
`BasicML/demo/` scripts to Adam (cost-surface trio and vanishing-gradient demo
included, both flagged non-mechanical); for the trio keep the one-cycle lr
schedule, drop momentum scheduling, rename "Momentum Path" -> "Optimizer Path".

## Action

On `feature/TODO-0009-more-optimizers`, all 8 `BasicML/demo/` training scripts:

- Mechanical swap (`from basicml.optim.momentum import Momentum` -> `adam import
  Adam`; remove `MOMENTUM`; `Adam(model.parameters(), lr=LEARN_RATE)`; retune
  lr): `plot_dynamic_decision_boundary.py` (0.08->0.01),
  `plot_layer_transformations.py` (0.08->0.01),
  `plot_dynamic_layer_morphing.py` (0.20->0.01; docstring "momentum SGD" ->
  "the Adam optimizer"), `plot_dynamic_mlp_graph.py` (0.05->0.005; keeps
  `regularizer=reg`).
- `plot_dynamic_vanishing_gradient.py`: swap to Adam (0.10->0.003); added a
  docstring paragraph noting Adam rescales the step by gradient RMS (curves land
  closer than under SGD) but the vanishing is unchanged and still visible in
  panel 4.
- Cost-surface trio `plot_dynamic_linear.py` / `plot_dynamic_logistic.py` /
  `plot_dynamic_3d_logistic.py`: `OneCycle` reduced to lr-only (`at()` returns
  `float`; dropped `momentum` field + `MOM_CYCLE` const), `optimizer.lr =
  schedule.at(...)`, `Momentum` -> `Adam`, trace label "Momentum Path" ->
  "Optimizer Path", module docstrings updated. `plot_dynamic_linear` lr cycle
  `(0.005,0.08,0.001)` -> `(0.02,0.20,0.005)`; logistic/3d kept `(0.05,0.4,0.01)`.

## Decision

The user picked every group after being shown that the trio and
vanishing-gradient demos are pedagogically tuned around Momentum. For the trio,
kept the warmup/anneal lr visualization (Adam's `lr` is mutable) and dropped
only the momentum triple, per the user's choice. For vanishing-gradient, kept
the demo honest with a docstring note rather than silently changing what the
panels imply. lrs retuned down (~0.01, Adam's per-parameter RMS scaling makes
the old SGD-sized steps of 0.05-0.2 diverge).

## Conclusion

Fixed. All 8 demos run headless (`MPLBACKEND=Agg python3.13 ...`) and render:
decision_boundary 98.0% acc, layer_transformations loss 0.0141, layer_morphing
100% acc, mlp_graph 95-96% val acc (plain / Dropout+L2), vanishing_gradient both
nets train, linear cost 0.0005 (early stop @574), logistic cost 0.036, 3d cost
0.046. `pyrefly check` -- 9 pre-existing errors, none new. train_regularization.py
and ref_code.py deliberately left on Momentum (out of scope: the former is about
L1/L2, the latter is a reference scratch file).
