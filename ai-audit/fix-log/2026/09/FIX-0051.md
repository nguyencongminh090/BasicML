---
id: FIX-0051
timestamp: 2026-09-12T21:00:00+07:00
todo_id: TODO-0030
---

## Prompt

"Implement Autograd" → clarified into: start a new sibling project `AdvanceML/` in modern C++ with a real autograd engine, backed by oneDNN for AVX-512 VNNI, alongside (not replacing) BasicML. This session's scope: locate scope, update `CLAUDE.md`, scaffold the project skeleton.

## Action

- Added `## AdvanceML` section to root `CLAUDE.md` (purpose, build commands, architecture direction, code style), renamed BasicML-specific section headers to `## BasicML — ...` for clarity, and noted the WIP-cap exception process in the Git-branching section.
- Created `AdvanceML/{CMakeLists.txt, README.md}`, `include/advanceml/{version.hpp, tensor.hpp}`, `src/version.cpp`, `tests/{CMakeLists.txt, smoke_test.cpp}`.
- Added `AdvanceML/build/` and `compile_commands.json` to root `.gitignore`.
- Filed `ai-audit/todo/TODO-0030.md` (in-progress, WIP-cap exception per explicit user choice), `ai-audit/instructions/TODO-0030.md`, and the `ai-audit/todo/INDEX.md` row.
- Branched `feature/TODO-0030-advanceml-scaffold` off `dev`.
- Verified: `find_package(dnnl CONFIG REQUIRED)` after discovering Debian's `libdnnl-dev` has no `.pc` file (initial `pkg_check_modules` attempt failed); `cmake -B AdvanceML/build -S AdvanceML -G Ninja` configures clean, `cmake --build AdvanceML/build` builds clean, `ctest --test-dir AdvanceML/build` passes the placeholder Catch2 smoke test.

## Decision

C++20/23 + oneDNN + CMake/Ninja + Catch2, run alongside BasicML rather than replacing it — all confirmed with the user via `AskUserQuestion` before scaffolding (see `ai-audit/instructions/TODO-0030.md` for full rationale, including why hand-written kernels aren't expected to beat oneDNN/PyTorch on raw GEMM throughput). `sudo apt install libdnnl-dev` was run by the user directly since this session's shell can't authenticate an interactive sudo prompt.

## Conclusion

Fixed / scaffold complete and verified end-to-end. Follow-up TODOs (not yet allocated) will implement the actual Tensor/autograd graph and oneDNN-backed ops — explicitly out of scope for TODO-0030.
