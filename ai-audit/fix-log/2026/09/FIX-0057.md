---
id: FIX-0057
timestamp: 2026-09-17T05:17:57+07:00
todo_id: TODO-0037
---

## Prompt

User: "DO TODO-0034". TODO-0034 is the AdvanceML component-library-parity umbrella (tracking entry only); asked the user which of its still-open sub-areas to pick up (areas 2 and 4 were already done via TODO-0035/TODO-0036) and whether to grant the WIP-cap exception again (TODO-0011 and TODO-0024 both already in-progress). User picked area 3, `CrossEntropyLoss`, and approved the exception.

## Action

- Added `cross_entropy_loss` op to `AdvanceML/include/advanceml/ops.hpp` / `AdvanceML/src/ops.cpp`: `loss = -mean_i(sum_j(target_ij * log(clip(pred_ij))))` with `[1e-15, 1-1e-15]` clipping and an autograd `backward_fn`.
- Added `CrossEntropyLoss` class to `AdvanceML/include/advanceml/nn/loss.hpp` / `AdvanceML/src/nn/loss.cpp`, a thin `Loss` wrapper matching `MSELoss`'s shape.
- Added tests to `AdvanceML/tests/nn_test.cpp`: a manual-computation + numerical-gradient check (`check_loss_gradient` helper), and a `Sequential`/`Softmax`/`CrossEntropyLoss`/`SGD` training-loss-decrease check on a small 3-class linearly separable task.
- Filed sub-TODO `TODO-0037` (opened and closed same session, archived directly) under the `TODO-0034` umbrella; appended a dated section to `ai-audit/instructions/TODO-0034.md`.
- Verified: `cmake --build AdvanceML/build` succeeds; `ctest --test-dir AdvanceML/build` passes 15/15 (both new cases included).

## Decision

Matched `basicml.nn.loss.CrossEntropyLoss`'s unfused contract (pred already softmax-probabilities, not raw logits) rather than a fused log-softmax+NLL op, for parity with BasicML's existing semantics and because `Softmax` already exists as its own `advanceml` op/Module from TODO-0035 — introducing a second, fused cross-entropy path would duplicate that logic. Followed the existing `mse_loss`/`MSELoss` op+wrapper pattern and the `nn_test.cpp` numerical-gradient-check convention (extended with a two-tensor-input variant, `check_loss_gradient`) rather than inventing a new test style.

## Conclusion

Fixed/landed. Build and full test suite pass (15/15, including the 2 new cases). Follow-ups: `TODO-0034` umbrella still has area 1 (CNN layers, its original priority-1 motivation), area 5 (datasets/metrics), and area 6 (MNIST example) open.
