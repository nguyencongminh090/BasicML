---
id: FIX-0053
timestamp: 2026-09-13T05:39:32+07:00
todo_id: TODO-0032
---

## Prompt

"DO task-0032" — implement AdvanceML's `Module`/`Layer` API on top of the proven autograd engine (TODO-0031). Scope narrowed via AskUserQuestion to the first slice only: `Module`/`Linear`/`ReLU`/`Sequential` (Loss/Optimizer refactor split to TODO-0033), with a WIP-cap exception (3rd in-progress slot) granted by the user since TODO-0011/TODO-0024 were already occupying the normal 2-slot cap.

## Action

Branched `feature/TODO-0032-module-api` off `dev`. Added:
- `AdvanceML/include/advanceml/nn/module.hpp` + `src/nn/module.cpp` — abstract `Module` base (`forward`, `parameters()`, `operator()`).
- `AdvanceML/include/advanceml/nn/linear.hpp` + `src/nn/linear.cpp` — `Linear` (Xavier-uniform `w`, zero `b`, forward via existing `matmul`+`operator+`).
- `AdvanceML/include/advanceml/nn/relu.hpp` + `src/nn/relu.cpp` — parameterless `ReLU` wrapping the existing `relu` op.
- `AdvanceML/include/advanceml/nn/sequential.hpp` + `src/nn/sequential.cpp` — `Sequential` container chaining child `Module`s and concatenating their `parameters()`.
- `AdvanceML/tests/nn_test.cpp` — rebuilds the TODO-0031 XOR MLP using `Sequential`/`Linear`/`ReLU` (still with raw `mse_loss`/`SGD`) and checks `Sequential::parameters()` ordering.
- Wired new sources into `AdvanceML/CMakeLists.txt` and the new test file into `AdvanceML/tests/CMakeLists.txt`.

## Decision

Mirrored BasicML's `Module`/`Linear`/`Sequential` shape in C++ idiom (per the TODO's own direction) rather than inventing a different layering. Kept `Loss`/`Optimizer` abstractions out of this slice — TODO-0032's own notes flagged the full scope as too large for one session, so it split into TODO-0033 rather than risk a half-finished `Loss`/`Optimizer` hierarchy alongside the model API.

## Conclusion

Fixed/implemented. `cmake --build AdvanceML/build` succeeds; `ctest --test-dir AdvanceML/build --output-on-failure` passes all 5 tests (3 pre-existing + 2 new), including the TODO-0031 tests unmodified. Follow-up: TODO-0033 (Loss/Optimizer refactor, `open`, not started).
