---
id: TODO-0010
status: in-progress
source: user-report
created: 2026-09-03
updated: 2026-09-03
priority: medium
tags: [loss, demo, teaching]
code_author: both
ai_role: implement + advise
---

## Description

Add the absolute (MAE / L1) loss to the library and an animated demo that shows
why a constant-magnitude gradient is a poor fit for plain gradient descent. Also
fold in a cleanup of `plot_dynamic_linear.py` (drop the one-cycle LR schedule +
early-stopping, back to a plain fixed-lr loop matching `examples/train_linear.py`)
and the small `plot_ewa.py` EWA teaching demo.

## Requirement

- `basicml/nn/loss.py`: `AbsoluteLoss` with forward `mean|y_pred - y_true|` and
  the correct subgradient `sign(y_pred - y_true) / n`; core stays bare.
- `demo/plot_dynamic_linear_abs.py`: 5-panel animation like
  `plot_dynamic_linear.py` but on the MAE loss + MAE cost surface, with a red
  current-position marker on every trajectory panel; SGD, fixed lr, visibly
  jitters near the optimum.
- `demo/plot_dynamic_linear.py`: plain fixed-lr Adam loop, no schedule / early
  stop.
- All scripts run headless; `pyrefly check` clean apart from the known
  matplotlib `set_3d_properties` false positive.

## Notes

See [instructions/TODO-0010.md](../instructions/TODO-0010.md) for session-by-session solution notes.
