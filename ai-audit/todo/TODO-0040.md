---
id: TODO-0040
status: in-progress
source: user-report
created: 2026-09-17
updated: 2026-09-17
priority: high
tags: [advanceml, c++, cnn, mnist, example]
code_author: both
ai_role: design + implement
---

## Description

Sixth and final sub-TODO of the [TODO-0034](../instructions/TODO-0034.md) umbrella, covering area 6: a runnable MNIST CNN example for AdvanceML, analogous to `BasicML/examples/train_cnn_mnist.py`. This is the concrete proof of the CPU-throughput goal stated in TODO-0030's reasoning — AdvanceML should match or beat frameworks like PyTorch on small/medium CPU models by cutting interpreter/dispatch overhead around the same oneDNN kernels, not necessarily out-perform hand-tuned BLAS/oneDNN itself. Unblocked now that TODO-0038 (Conv2D/pooling/BatchNorm2D) and TODO-0039 (MNIST IDX loader/accuracy metric) are both merged into `dev`.

## Requirement

- A new `AdvanceML/examples/` target (e.g. `train_cnn_mnist.cpp`) building a small CNN (Conv2D/BatchNorm2D/ReLU/pooling stack, mirroring `BasicML/examples/train_cnn_mnist.py`'s architecture) on real MNIST via `datasets::load_mnist`, trained with `AdamW`, reporting `metrics::Accuracy` on a held-out split each epoch.
- Wired into `AdvanceML/CMakeLists.txt` as its own executable target (not part of the `advanceml_tests` Catch2 binary).
- A rough perf comparison against BasicML's `train_cnn_mnist.py` equivalent (wall-clock per epoch or total, same/comparable architecture and dataset split) — the concrete proof of the CPU-throughput goal.
- `cmake --build AdvanceML/build` + `ctest --test-dir AdvanceML/build --output-on-failure` stay green (existing tests unaffected by the new example target).
- Fix-log entry filed once landed; TODO-0034 umbrella closed out (all six areas done) in the same session if this completes it.

## Notes

See [instructions/TODO-0040.md](../instructions/TODO-0040.md) for session-by-session solution notes.
