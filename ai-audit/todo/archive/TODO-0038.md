---
id: TODO-0038
status: done
source: user-report
created: 2026-09-17
updated: 2026-09-17
priority: high
tags: [advanceml, cnn]
code_author: ai
ai_role: implement
---

## Description

Sub-TODO of [TODO-0034](TODO-0034.md) (AdvanceML umbrella), area 1: add CNN layers to
AdvanceML — `Conv2D`, pooling (Max/Avg), `Flatten`, `BatchNorm2D` — built on the
`Module`/autograd architecture landed in TODO-0032/0033, using oneDNN's conv and pooling
primitives behind the existing backend abstraction. This is the original motivation for
AdvanceML (CNN/Transformer-scale workloads BasicML's pure-NumPy design can't handle
efficiently on CPU).

Started as a **WIP-cap exception**: TODO-0011 and TODO-0024 are both already
in-progress (normal cap is 2). Approved by the user via `AskUserQuestion` in this
session, same pattern as TODO-0035/0036/0037.

## Requirement

- `Conv2D` layer implemented via oneDNN convolution primitive, integrated with the
  autograd graph (forward + backward through the graph, no manual `backward()`).
- Pooling layer(s) (at least MaxPool2D; AvgPool2D if straightforward) via oneDNN
  pooling primitives.
- `Flatten` layer to bridge conv output to fully-connected layers.
- `BatchNorm2D` layer.
- Each new op follows AdvanceML's existing `Backend`-abstraction convention (add new
  ops as new classes, not branches in existing ones).
- Public headers get full Doxygen-style docstrings per AdvanceML code style; `.cpp`
  files may use sparse why-comments only.
- Catch2 tests added under `AdvanceML/tests/` mirroring `AdvanceML/src/` layout.
- `cmake --build` and `ctest` pass.
- Branch: `feature/TODO-0038-cnn-layers` off `dev`.

## Notes

See [instructions/TODO-0038.md](../instructions/TODO-0038.md) for session-by-session solution notes.
