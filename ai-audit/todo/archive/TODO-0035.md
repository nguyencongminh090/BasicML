---
id: TODO-0035
status: done
source: user-report
created: 2026-09-13
updated: 2026-09-13
priority: high
tags: [advanceml, c++, activations, autograd]
code_author: both
ai_role: design + implement
---

## Description

Sub-TODO of the [TODO-0034](../TODO-0034.md) umbrella (AdvanceML component-library parity with BasicML). Adds more activation `Module`s to AdvanceML's `nn` package, mirroring `basicml/nn/activation.py`: at minimum `Sigmoid`, `LeakyReLU`, `GELU`, and `Softmax`. `Softmax` is a prerequisite for the future `CrossEntropyLoss` sub-TODO. AdvanceML currently only has `ReLU`; this closes gap #2 from TODO-0034's landing-priority list.

**WIP-cap exception:** filed and started as a 3rd in-progress item even though TODO-0011 and TODO-0024 were already in-progress (normal cap is 2). The user explicitly approved this exception in the session that filed this TODO, choosing to start on activations (area 2) ahead of CNN layers (area 1, the umbrella's suggested first area) despite the assistant recommending area 1. See [instructions/TODO-0035.md](../../instructions/TODO-0035.md) for the session note.

## Requirement

- New activation classes live under `AdvanceML/include/advanceml/nn/` + `AdvanceML/src/nn/`, following the existing `Module` interface and autograd conventions established by `ReLU`/`Linear` (TODO-0032/0033) — no manual `backward()`, ops build the computation graph like the rest of AdvanceML. — met (`sigmoid`/`leaky_relu`/`gelu`/`softmax` ops + `Sigmoid`/`LeakyReLU`/`GELU`/`Softmax` wrappers)
- Each activation gets Doxygen docstrings on its public declarations per the AdvanceML code-style rules in `CLAUDE.md`, stating the forward/backward math. — met
- Each activation with a non-trivial gradient gets a numerical-gradient-check test in `AdvanceML/tests/nn_test.cpp` (same pattern as existing tests), not just a smoke test. — met (4 new tests, plus a Softmax row-sum check)
- `cmake --build AdvanceML/build` + `ctest --test-dir AdvanceML/build --output-on-failure` stay green. — met, 9/9 tests
- Branch: `feature/TODO-0035-activations` off `dev`, merged back to `dev` only. — met

## Notes

See [instructions/TODO-0035.md](../../instructions/TODO-0035.md) for session-by-session solution notes.
