---
id: TODO-0037
status: done
source: user-report
created: 2026-09-13
updated: 2026-09-13
priority: high
tags: [advanceml, nn, loss]
code_author: ai
ai_role: implement
---

## Description

TODO-0034 sub-area 3 (losses): add `CrossEntropyLoss` to AdvanceML's `advanceml::Loss` hierarchy, matching `basicml.nn.loss.CrossEntropyLoss`'s contract -- unfused with softmax, `pred` is expected to already be a probability distribution (e.g. `Softmax`'s output) and `target` a one-hot distribution of the same shape. Unblocked by TODO-0035 landing `Softmax`.

## Requirement

- `cross_entropy_loss` op added to `include/advanceml/ops.hpp` / `src/ops.cpp`: `loss = -mean_i(sum_j(target_ij * log(clip(pred_ij))))`, clipping `pred` to `[1e-15, 1 - 1e-15]` to avoid `log(0)`, with an autograd-compatible `backward_fn`.
- `CrossEntropyLoss` class in `include/advanceml/nn/loss.hpp` / `src/nn/loss.cpp`, a thin `Loss` wrapper around the op (same shape as `MSELoss`).
- Covered by new Catch2 tests in `AdvanceML/tests/nn_test.cpp`: a manual-computation + numerical-gradient check, and a `Sequential`/`Softmax`/`CrossEntropyLoss`/`SGD` training-loss-decrease check on a small 3-class linearly separable task (same pattern as the existing XOR-MLP training tests).
- `cmake --build AdvanceML/build` and `ctest --test-dir AdvanceML/build` pass.

## Notes

WIP-cap exception: TODO-0011 and TODO-0024 were already in-progress when this was started (user-approved via AskUserQuestion, same pattern as TODO-0035/TODO-0036).

See [instructions/TODO-0037.md](../instructions/TODO-0037.md) for session-by-session solution notes.
