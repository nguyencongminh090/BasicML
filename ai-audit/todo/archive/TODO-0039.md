---
id: TODO-0039
status: done
source: user-report
created: 2026-09-17
updated: 2026-09-17
priority: high
tags: [advanceml, c++, datasets, metrics, mnist]
code_author: both
ai_role: design + implement
---

## Description

Sub-TODO of the [TODO-0034](../instructions/TODO-0034.md) umbrella, covering area 5: supporting utilities for AdvanceML. Add a C++ MNIST loader for the IDX file format (no Python dependency at runtime, unlike BasicML's `sklearn.datasets.fetch_openml` fetcher) and a minimal metrics helper (accuracy at least), mirroring `basicml/datasets/` and any equivalent metrics helper in BasicML. This unblocks area 6 (a runnable MNIST CNN example), which needs both a real dataset and a way to report something other than raw loss.

## Requirement

- An IDX-format MNIST loader (images + labels, both the ubyte train/test file pairs) under `AdvanceML/include/advanceml/datasets/` + `AdvanceML/src/datasets/`, returning tensors usable directly by `nn::Module`/training loops.
- A minimal metrics helper (accuracy at least) under `AdvanceML/include/advanceml/metrics/` + `AdvanceML/src/`.
- Every new public declaration carries Doxygen docstrings per `CLAUDE.md`'s AdvanceML code-style rules.
- Unit tests (Catch2, mirroring `AdvanceML/tests/`) covering the IDX parser (e.g. against a small synthetic/fixture file) and the accuracy metric.
- `cmake --build AdvanceML/build` + `ctest --test-dir AdvanceML/build --output-on-failure` stay green.
- Fix-log entry filed once landed, per TODO-0034's requirement to capture progress per sub-area.

## Notes

See [instructions/TODO-0039.md](../instructions/TODO-0039.md) for session-by-session solution notes.
