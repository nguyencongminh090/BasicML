---
id: TODO-0036
status: done
source: user-report
created: 2026-09-13
updated: 2026-09-13
priority: high
tags: [advanceml, optim]
code_author: ai
ai_role: implement
---

## Description

TODO-0034 sub-area 4 (optimizers): add `Momentum`, `Adam`, and `AdamW` optimizers to AdvanceML's `advanceml::Optimizer` hierarchy, matching the shape of `basicml.optim.momentum`/`adam`/`adamw` conceptually (not a port -- operates on `Tensor::grad()`/`Tensor::zero_grad()` like `advanceml::SGD` already does).

## Requirement

- `include/advanceml/optim/{momentum,adam,adamw}.hpp` + matching `src/optim/*.cpp`, each a class deriving from `Optimizer` with Doxygen header comments per AdvanceML's "library edge" convention.
- `Momentum`: per-parameter velocity buffer, `velocity = momentum * velocity + grad; param -= lr * velocity`.
- `Adam`/`AdamW`: per-parameter first/second moment buffers with bias correction; `AdamW` applies decoupled weight decay (not L2-into-gradient).
- Registered in `AdvanceML/CMakeLists.txt` and covered by new Catch2 tests (`AdvanceML/tests/optim_test.cpp`) exercising a step's numeric effect (e.g. momentum accumulation) and/or a small training-loss-decrease check, added to `AdvanceML/tests/CMakeLists.txt`.
- `cmake --build AdvanceML/build` and `ctest --test-dir AdvanceML/build` pass.

## Notes

WIP-cap exception: TODO-0011 and TODO-0024 were already in-progress when this was started (user-approved via AskUserQuestion, same pattern as TODO-0035).

See [instructions/TODO-0036.md](../instructions/TODO-0036.md) for session-by-session solution notes.
