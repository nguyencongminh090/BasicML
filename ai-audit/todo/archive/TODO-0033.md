---
id: TODO-0033
status: done
source: user-report
created: 2026-09-13
updated: 2026-09-13
priority: high
tags: [advanceml, c++, loss, optimizer, api]
code_author: both
ai_role: design + implement
---

## Description

Split off from [TODO-0032](TODO-0032.md) (Module/Layer/Loss/Optimizer API): once `Module`/`Linear`/`ReLU`/`Sequential` exist, bring the `Loss` and `Optimizer` abstractions up to the same shape. `Loss` is an abstract base with `operator()(pred, target) -> Tensor` (no separate `backward()` needed — the returned scalar `Tensor` already ties back into the autograd graph); wrap the existing `mse_loss` free function as `MSELoss : Loss`. `Optimizer` is an abstract base with `step()`/`zero_grad()` over a `vector<Tensor>`; refactor the existing `SGD` to derive from it without changing its behavior.

## Requirement

- New `AdvanceML/include/advanceml/nn/loss.hpp` (+ `src/nn/loss.cpp`) and `AdvanceML/include/advanceml/optim/optimizer.hpp` (+ `src/optim/optimizer.cpp`); `SGD` moves under `optim/` and derives from `Optimizer`. — met
- `SGD`'s existing tests (from TODO-0031) keep passing unmodified in behavior (include path may change). — met
- A new/extended end-to-end test rebuilds the TODO-0031/TODO-0032 XOR MLP using `Sequential`/`Linear`/`ReLU`/`MSELoss`/`SGD` end-to-end (the full proof the original TODO-0032 requirement asked for), showing the same loss-decrease result. — met
- All new public declarations carry Doxygen docstrings per the AdvanceML code-style rules. — met
- `cmake --build AdvanceML/build` succeeds and `ctest --test-dir AdvanceML/build --output-on-failure` passes. — met, 5/5 tests

## Notes

See [instructions/TODO-0032.md](../../instructions/TODO-0032.md) for the scope-split rationale, and [instructions/TODO-0033.md](../../instructions/TODO-0033.md) for this TODO's own implementation session.
