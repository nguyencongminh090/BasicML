---
id: TODO-0031
status: done
source: user-report
created: 2026-09-12
updated: 2026-09-13
priority: high
tags: [advanceml, c++, autograd, mlp]
code_author: both
ai_role: design + implement
---

## Description

Follow-up to [TODO-0030](TODO-0030.md) (AdvanceML scaffold, done): implement the actual autograd engine — a `Tensor`/`Node` define-by-run computation graph with `backward()` — and prove it end-to-end with a tiny MLP training loop, rather than jumping straight to CNN/Transformer ops. Scalar type is float32-only for this pass; GEMM goes through oneDNN (`dnnl::matmul`) to exercise this CPU's AVX-512 VNNI.

Ops in scope: `operator+`/`operator*` (bias-add/scaling only, no general broadcasting), `matmul` (2D only), `relu`, `mse_loss`, and a minimal `SGD` step. Out of scope (future TODOs): Conv2D/pooling, general broadcasting, in-place ops, no-grad mode, other dtypes, any BasicML model port, Python bindings.

## Requirement

- `Tensor`/`TensorImpl`/`Node` implemented per the PyTorch-style shared_ptr design agreed with the user (see instructions note); `Tensor::backward()` performs a reverse topological traversal accumulating into `.grad`. — met
- `cmake --build AdvanceML/build` succeeds with the new sources. — met
- `ctest --test-dir AdvanceML/build --output-on-failure` passes, including:
  - a numerical-gradient-check test (central-difference vs. analytic `.grad` on a small graph),
  - an end-to-end 2-layer MLP training test showing real loss decrease via the new `SGD`. — met, all 3 tests pass (re-verified 2026-09-13)
- All new public declarations in `tensor.hpp`/`ops.hpp`/`optim.hpp` carry Doxygen-style docstrings stating the math (forward + backward formulas), per the AdvanceML code-style rules in `CLAUDE.md`. — met

## Notes

See [instructions/TODO-0031.md](../../instructions/TODO-0031.md) for session-by-session solution notes, including the WIP-cap exception rationale (continued from TODO-0030). Merged to `dev` in commit `005c82d`; closes the WIP-cap exception opened for TODO-0030.
