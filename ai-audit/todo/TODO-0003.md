---
id: TODO-0003
status: in-progress
source: user-report
created: 2026-09-02
updated: 2026-09-02
priority: medium
tags: [examples, regularization, overfitting]
code_author: ai
ai_role: implement
---

## Description

Follow-up to [TODO-0002](TODO-0002.md): add a self-contained example under `BasicML/examples/`
that demonstrates regularization as an overfitting control. Train an over-capacity MLP on a
small, noisy `make_moons` training set and compare an unregularized run against L2 (and L1)
runs, reporting the train/validation generalization gap and plotting the decision boundaries.

## Requirement

- New script `BasicML/examples/train_regularization.py`, runnable directly (`python ...`),
  self-inserting `BasicML/` onto `sys.path` like the other examples.
- Uses `basicml.datasets.make_moons`; small noisy train split + larger held-out validation set.
- Over-capacity `Sequential` MLP (ReLU hidden layers, `he` init, `Sigmoid` head), `BinaryCrossEntropy`.
- Trains: no-reg vs `L2` vs `L1`, all else equal; wires `regularizer=` into the optimizer.
- Prints train vs val loss and accuracy per run + the generalization gap; matplotlib decision
  boundary plot (guarded by a `SHOW_PLOT` flag).
- Accuracy / train-val split computed inline (no library changes this round).
- `pyrefly check` clean; existing `examples/` + `demo/` scripts still run.

## Notes

See [instructions/TODO-0003.md](../instructions/TODO-0003.md) for session-by-session solution notes.
