# TODO-0031 — AdvanceML: autograd engine milestone 1 (tiny MLP)

## 2026-09-12

Continuation of TODO-0030 (scaffold, done) — user asked to "start" TODO-0030, clarified to mean beginning the actual autograd engine work, tracked as its own TODO (TODO-0031) since TODO-0030's scaffold deliverable was already complete/verified.

**Scope decisions, confirmed with the user via AskUserQuestion:**
- First milestone proves the whole mechanism on a tiny MLP training loop (Tensor/Node/backward + matmul/relu/mse_loss/SGD), rather than jumping straight to Conv2D/CNN — de-risks the graph design before scaling up.
- float32-only for this pass; templating on scalar type deferred.

**Design (full plan in `/home/ngmint/.claude/plans/snug-leaping-hickey.md`, approved by the user):**
- `Tensor` = value-type handle wrapping `shared_ptr<TensorImpl>` (data, shape, grad, requires_grad, grad_fn).
- `Node` = `{inputs: vector<shared_ptr<TensorImpl>>, backward_fn: function<vector<Tensor>(const Tensor&)>}`, PyTorch-style — mirrors BasicML's "forward caches what backward needs" convention, just automated instead of hand-driven per [[../../CLAUDE.md]]'s AdvanceML section.
- `Tensor::backward()` does reverse-DFS over `grad_fn` chains (visited-by-pointer-identity), accumulating into `.grad`.
- `matmul` calls `dnnl::matmul` for forward (exercises AVX-512 VNNI); backward is two more matmuls, same shape as BasicML's `Linear.backward`.

**WIP-cap exception:** continues the exception opened for TODO-0030 (see `ai-audit/instructions/TODO-0030.md`) — the user chose to keep it open rather than treat this as a second, separate exception, since it's the same AdvanceML initiative.

**Branch discipline:** before this TODO's implementation began, the pre-existing unrelated uncommitted backlog (TODO-0019 through TODO-0029, CNN/BatchNorm/Muon/LeNet work from prior sessions) was committed and merged to `dev` first, one branch per TODO per the user's explicit choice ("strict: one branch per TODO"), plus one chore commit for the shared `CLAUDE.md`/index-file docs. Then TODO-0030's scaffold was committed/merged cleanly. `feature/TODO-0031-autograd-engine` branches off the resulting clean `dev`.

Verification: `cmake --build AdvanceML/build` + `ctest --test-dir AdvanceML/build --output-on-failure`, including a numerical-gradient-check test and an MLP loss-decrease test.
