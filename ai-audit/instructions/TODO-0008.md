# Instructions — TODO-0008

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-02 23:21

**Discussed:** User asked to add more activation functions to
`BasicML/basicml/nn/activation.py`, having already written the base
`Activation`/`Sigmoid`/`ReLU`/`Tanh`. Via AskUserQuestion the user picked the
full scope: core set + parametric extras + simple extras.

**Decision:** Added 12 layers on `feature/TODO-0008-more-activations` (off
`dev`): `Identity`, `LeakyReLU`, `PReLU`, `ELU`, `SELU`, `Softplus`, `GELU`
(tanh approximation), `Swish` (SiLU when beta=1), `Mish`, `Hardtanh`,
`Hardsigmoid`, `Softmax` (row-wise, `axis=-1`, closed-form
`f * (g - sum(g*f))` backward). `PReLU` carries a trainable `Tensor` slope,
overrides `parameters()`, and accumulates `dL/da` with `+=` like `Linear`.
Existing `Sigmoid`/`ReLU`/`Tanh` gained docstrings, unchanged behaviour.

**Verification:** central-difference gradient check for all 14 layers
(max abs error ~1e-9); PReLU input + scalar/per-feature slope gradients checked;
`Sequential(Linear, GELU, Linear, Softmax)` forward/backward smoke test;
`pyrefly check` — no new errors (9 pre-existing errors in `ref_code.py` /
`plot_dynamic_*` untouched).

**Next step:** commit via `/make-commit`, merge to `dev`, close TODO.

## 2026-09-02 23:40

**Discussed:** User note — "No comments in my core (basicml)", and to update
CLAUDE.md.

**Decision:** Stripped the `#` comments from the new `activation.py` (math moved
into docstrings). Rewrote the CLAUDE.md `## Code style` comment bullet: the core
library carries zero inline comments (everything explanatory goes in the
Google-style docstring); `examples/`/`demo/` may still use sparse
`why`-comments. Pre-existing comments elsewhere in the core
(`datasets/synthetic.py`, `visualize/decision_boundary.py`) left as-is — not in
scope here.

**Next step:** unchanged — commit, merge to `dev`, close TODO.

## 2026-09-02 23:55

**Discussed:** User: "clean docstring" in `activation.py`.

**Decision:** Tightened every docstring — dropped the RST `` `` `` / `:class:`
markup and the extra background prose (EfficientNet/transformer asides, etc.),
kept the mandatory Google structure (summary line with the formula in plain
text + Args/Returns/Raises), added missing `Raises: ValueError` entries on the
constructors that validate. No code change. Gradient check + pyrefly re-run,
both unchanged.

## 2026-09-03 00:10

**Discussed:** User: docstrings should be "empty as other files". Confirmed the
rest of the core (`nn/linear.py`, `nn/loss.py`, `nn/init.py`, `optim/*.py`,
`tensor.py`) carries **no** docstrings and no comments — only `datasets/` and
`visualize/` are documented.

**Decision:** Stripped every docstring and comment from `activation.py`; kept
the `RuntimeError` forward-order guards. Rewrote the CLAUDE.md code-style
bullets: core library modules stay bare (no comments, no docstrings);
Google-style docstrings are required only in `basicml/datasets/` and
`basicml/visualize/`; `examples/`/`demo/` may carry sparse `why`-comments and
script docstrings. Gradient check + pyrefly re-run — unchanged.

**Next step:** commit, merge to `dev`, close TODO.
