# TODO-0035 — AdvanceML: more activation Modules

## 2026-09-13

Filed as a sub-TODO of the [TODO-0034](../todo/TODO-0034.md) umbrella. User was asked (via AskUserQuestion) how to handle the fact that the ai-audit WIP cap (2 in-progress) was already full with TODO-0011 and TODO-0024, and which sub-area of TODO-0034 to start first.

User's answers:
- WIP cap: **grant an exception** — proceed with a 3rd in-progress item rather than pausing TODO-0011/0024 or only filing sub-TODOs without branching.
- Sub-area: **activations** (area 2 — Sigmoid, LeakyReLU, GELU, Softmax), not CNN layers (area 1), even though the umbrella's suggested landing order and the assistant's own recommendation put CNN layers first. Reason not stated by the user beyond the direct choice.

Proceeding to implement on branch `feature/TODO-0035-activations` off `dev`.

Before branching, found the `feature/TODO-0032-module-api` branch still had uncommitted TODO-0032/0033 work (Module/Linear/ReLU/Sequential + Loss/Optimizer) in the working tree from a prior session. Asked the user how to handle it; chose to commit it now via `commit-task`. Split into a `feat` commit (AdvanceML code) and a `chore` commit (ai-audit bookkeeping), then merged that branch into `dev` before branching `feature/TODO-0035-activations` off the now-current `dev`.

Implemented `sigmoid`/`leaky_relu`/`gelu`/`softmax` ops in `ops.hpp`/`ops.cpp` (each with its own autograd `Node`) and `Sigmoid`/`LeakyReLU`/`GELU`/`Softmax` `Module` wrappers under `nn/`, wired into `CMakeLists.txt`. Added a numerical-gradient-check test per activation in `nn_test.cpp` plus a `Softmax` row-sum-to-1 check, reusing `autograd_test.cpp`'s central-difference pattern. `cmake --build` + `ctest` pass all 9 tests (5 pre-existing + 4 new). See FIX-0055.

Marked `done` and archived same session (all requirements met, branch ready to merge to `dev`).
