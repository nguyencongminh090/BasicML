# Instructions — TODO-0007

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-02 20:45

**Discussed:** User asked to add code-style rules to CLAUDE.md (English code +
comments, Clean-Code comment placement, SOLID, function docstring format). When
asked about scope vs the existing "docs/commits in Vietnamese" line, user said
they intend to internationalize the whole repo to English, and want the two new
demo files plus all remaining Vietnamese code comments translated.

**Decision:**
- CLAUDE.md updated now (FIX-0011): rewrote the language line in "What this repo
  is"; added a `## Code style` section — English-only `.py`, Clean-Code comments,
  Google-style docstrings (pointing at `datasets/synthetic.py` and
  `visualize/decision_boundary.py` as the in-repo examples), SOLID mapped to this
  library's `Module`/`Loss`/`Optimizer` abstractions.
- Repo-wide comment translation is a large job → this TODO stays `open` (WIP cap
  is already at 2: TODO-0005, TODO-0006). Do it module-by-module later.
- The two demo files (TODO-0005 / TODO-0006) are translated now under those
  TODOs since they were uncommitted when the rule landed.

**Open question for the user:** does the English migration also cover
`README.md`, `BasicML/logs/`, and the language of *new* commit messages? Not
decided yet.

**Next step:** pick a module and start translating; confirm the open question.

## 2026-09-02 21:31

**Discussed:** User answered the open question — the English migration **does**
cover `README.md`, `BasicML/logs/`, and the language of *new* commit messages.
Then asked to rewrite `README.md` in English, supplying intro framing (learn
ML/DL by building from scratch in NumPy; PyTorch-inspired for cohesion / low
coupling).

**Decision:** (via AskUserQuestion) translate **and** refresh README to match the
current codebase, rebuild the roadmap table from the ai-audit backlog, and remove
all LaTeX math. Done in FIX-0013 — README is now English-only, accurate to the
current library surface, no math.

**Still pending under this TODO:**
- `examples/train_linear.py`, `train_logistic.py`, `check_gradients.py` — Vietnamese docstrings.
- `BasicML/demo/layer_space_transformation.ipynb` — Vietnamese markdown cells.
- `BasicML/logs/` older entries — now in scope.
- New commit messages: write in English going forward.

**Next step:** translate the remaining `examples/` docstrings and the notebook.

## 2026-09-02 21:49

**Discussed:** User shared an external "how pros write READMEs" analysis +
scorecard and asked to apply it to `README.md`.

**Decision:** (via AskUserQuestion) add **MIT** license; visuals = **demo table +
generated static PNGs** (no animation possible here). Done in FIX-0014:
- `LICENSE` (MIT) added.
- `assets/{layer_transformations,regularization,logistic_fit}.png` generated by
  running the real scripts with a patched `plt.show`.
- README restructured: badges → banner → highlights → Quick start → gradient
  verification → architecture (tree + component table) → demos (table + images)
  → roadmap → logs links → license.

**Still pending under this TODO:** Vietnamese docstrings in `examples/train_linear.py`,
`train_logistic.py`, `check_gradients.py` and every `demo/*.py`; the
`.ipynb` notebook; English commit messages going forward; `BasicML/logs/` are
already English.

**Next step:** translate `examples/` + `demo/` docstrings and the notebook.
