# Instructions — TODO-0007

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-02 20:45

**Discussed:** User asked to add code-style rules to CLAUDE.md (English code +
comments, Clean-Code comment placement, SOLID, function docstring format). When
asked about scope vs the existing "docs/commits in Vietnamese" line, user said
they intend to internationalize the whole repo to English, and want the two new
demo files plus all remaining Vietnamese code comments translated.

**Decision:**
- CLAUDE.md updated now (FIX-0011): rewrote the language line in "What this repo
  is"; added a `## Code style` section — English-only `.py`, Clean-Code comments,
  Google-style docstrings (pointing at `datasets/synthetic.py` and
  `visualize/decision_boundary.py` as the in-repo examples), SOLID mapped to this
  library's `Module`/`Loss`/`Optimizer` abstractions.
- Repo-wide comment translation is a large job → this TODO stays `open` (WIP cap
  is already at 2: TODO-0005, TODO-0006). Do it module-by-module later.
- The two demo files (TODO-0005 / TODO-0006) are translated now under those
  TODOs since they were uncommitted when the rule landed.

**Open question for the user:** does the English migration also cover
`README.md`, `BasicML/logs/`, and the language of *new* commit messages? Not
decided yet.

**Next step:** pick a module and start translating; confirm the open question.
