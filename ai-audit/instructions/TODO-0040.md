# Instructions — TODO-0040

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-17

**Discussed:** User said "DO Task-0034" again. TODO-0038 and TODO-0039 (finished, unmerged) were merged into `dev` first (see [FIX-0060](../fix-log/2026/09/2026-09-17_0808-merge-todo-0038-0039-into-dev.md)), unblocking area 6. User chose to merge both and start area 6 in the same go.
**Decision:** File TODO-0040 covering area 6: a runnable MNIST CNN example plus a rough perf comparison against `BasicML/examples/train_cnn_mnist.py`. Starting it exceeds the WIP cap again (TODO-0011 and TODO-0024 still both in-progress) — asked the user via `AskUserQuestion` earlier in this exchange as part of the combined merge+start question; approved.
**Next step:** Branch `feature/TODO-0040-mnist-cnn-example` off `dev`, add `AdvanceML/examples/train_cnn_mnist.cpp` (Conv2D/BatchNorm2D/ReLU/pooling stack + AdamW + Accuracy, using the TODO-0039 MNIST loader), wire it into CMake as its own executable, keep the test suite green, time it against BasicML's equivalent, then commit via `commit-task` and file a fix-log entry. If this closes all six areas, archive TODO-0034 itself too.

**Done same session.** Added `train_cnn_mnist.cpp` (same architecture/config as `BasicML/examples/train_cnn_mnist.py`, using `AvgPool2D(7,7)` in place of a dedicated global-pool layer) + `AdvanceML/scripts/prepare_mnist_idx.py` (one-time real-MNIST-to-IDX export via BasicML's cached `scikit-learn` fetch, no Python at runtime for the C++ side). Rough perf comparison at a reduced scale (2000/400 images, 3 epochs, isolated runs): BasicML ~32s wall-clock vs. AdvanceML ~117s -- AdvanceML currently ~3.7x slower, root-caused to `conv2d`/`max_pool2d`/`avg_pool2d`/`batch_norm2d` rebuilding their oneDNN primitive descriptors on every call instead of caching by shape (documented as a known limitation in `AdvanceML/README.md`, not fixed here -- out of this TODO's scope). Did not run the full 15000/2500/10-epoch config end to end (an isolated single-epoch timing projected ~50 minutes for all 10, too much session time for a "rough" comparison that wouldn't change the finding). 31/31 tests passing. See [FIX-0061](../fix-log/2026/09/2026-09-17_0848-advanceml-mnist-cnn-example.md).

All six areas of the TODO-0034 umbrella are now done. Archiving both TODO-0040 and TODO-0034.
