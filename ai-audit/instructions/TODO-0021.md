# Instructions — TODO-0021

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-11 09:15

**Discussed:** Follow-up to TODO-0020 (trained CNN + animated feature maps
on real MNIST): apply LeNet architecture to the demo. Two choices needed:
(1) how faithful to the original LeNet-5 (Tanh + AvgPool + 32x32-padded
input vs. a modernized ReLU/MaxPool version on 28x28 directly), (2) whether
to replace the existing demo's model or add a new file.
**Decision:** User chose "modernized LeNet-ish" (ReLU + MaxPool2D, no
32x32 padding, but keeping the original 6/16 conv channel counts and
120/84 FC sizes) and a new separate demo file rather than modifying
`plot_dynamic_cnn_feature_maps.py`.
**Next step:** none — implemented as
`BasicML/demo/plot_dynamic_lenet_feature_maps.py`, verified, and closed in
this session.

## 2026-09-11 09:40

**Discussed:** Same request as noted in TODO-0020's instructions — track
digit "8" instead of whatever sample index 0 happens to be.
**Decision:** Same `TARGET_DIGIT = 8` + `pick_sample()` pattern applied here
too, so both sibling demos stay consistent and comparable.
**Next step:** none — verified.
