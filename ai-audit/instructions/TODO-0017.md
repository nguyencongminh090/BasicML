# Instructions — TODO-0017

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-08 22:40

**Discussed:** User asked "training - add display with metrics?" and, when asked
how it should relate to the core, asked back whether the core lib supports
display or it goes through a helper. Answer given: the core stays bare and only
provides the `Metric` objects; a formatting/printing helper belongs in the
`basicml/visualize/` edge package. User wants it used like `train_linear.py` --
"besides display loss, display metrics as Torch do".

**Decision:**
- New `basicml/visualize/progress.py`: `format_metric_line(...)` pure formatter +
  `ProgressPrinter` (caller-supplied `dict[str, Metric]`, `.update()` per batch,
  `.end_epoch()` prints a PyTorch-style line and resets, optional `every=N`).
- Retrofit `examples/train_linear.py` only (loss + MAE/RMSE/R2). Leaving
  `train_logistic.py` etc. as a follow-up.
- WIP slot: freed by merging TODO-0016 into `dev` first (user approved).

**Next step:** Implement on `feature/TODO-0017-training-progress-display`.

## 2026-09-08 23:05

**Decision:** Implemented `basicml/visualize/progress.py` (`format_metric_line` +
`ProgressPrinter`), re-exported from `visualize/__init__.py`, retrofitted
`examples/train_linear.py` (loss + MAE/RMSE/R2, `every=20`). Metric values that
aren't scalar raise `TypeError`. See FIX-0037.

**Next step:** Example runs, `pyrefly` at baseline. Committed on the branch.
Awaiting user before merging to `dev`.
