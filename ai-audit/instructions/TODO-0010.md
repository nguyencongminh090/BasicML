---
todo_id: TODO-0010
---

## 2026-09-03 — initial implementation

Single session, committed as one `feat` on `feature/TODO-0010-absolute-loss`.

**Work (see fix-log for detail):**

- FIX-0022 — `plot_dynamic_linear.py`: removed `OneCycle` LR schedule +
  early-stopping, plain fixed-lr Adam loop.
- FIX-0023 — added `AbsoluteLoss` to `nn/loss.py` (initially with a deliberately
  wrong MSE-style gradient) + new `plot_dynamic_linear_abs.py` (copy of the
  linear demo, MAE loss + MAE cost surface via `l1_optimum` grid search).
- FIX-0024 — red current-position markers on every trajectory panel of the abs
  demo; w/b/J in the learning-curve title.
- FIX-0025 — diagnosed a user `SGD` `nan` overflow (Adam-sized lr on raw-gradient
  SGD); kept `SGD` + `lr=0.01`.
- FIX-0026 — `AbsoluteLoss.backward` switched to the correct subgradient
  `sign(y_pred - y) / n`; abs-demo docstrings/title reworded to a genuine L1
  demo that shows the fixed-lr jitter.

Also in the same commit: user hand-tweaked `examples/train_linear.py`
`LEARN_RATE` 0.1 -> 0.2 and `demo/plot_dynamic_linear.py` `LEARN_RATE` 0.1 -> 0.5;
`demo/plot_ewa.py` (EWA teaching demo, from the TODO-0009 optimizer thread) added.

**Verification:** all touched scripts run headless; `pyrefly check` clean apart
from the pre-existing matplotlib `Line2D.set_3d_properties` false positive.

**Done:** merged `feature/TODO-0010-absolute-loss` -> `dev` (merge edb6650,
feat 5c85e30); TODO-0010 closed and archived.
