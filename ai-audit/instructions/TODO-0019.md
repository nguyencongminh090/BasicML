# Instructions — TODO-0019

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-11 01:30

**Discussed:** Whether the convolution used for the feature-map/receptive-field
demo should be a new reusable `Conv2D` under `basicml/nn/`, or kept
self-contained in the demo script. Discovered mid-conversation that
`Conv2D`/`MaxPool2D`/`AvgPool2D`/`Flatten` already exist in `basicml/nn/`
(added by TODO-0018, already merged into `dev`) with full `Module`
forward/backward/parameters contracts.
**Decision:** User chose to build the demo on the existing `basicml/nn`
modules (`Conv2D`, `MaxPool2D`, `ReLU`) rather than duplicating conv/pool
logic inside the demo file — no new library code needed.
**Next step:** Implement `BasicML/demo/plot_feature_maps_receptive_field.py`:
synthetic edge image -> small conv/pool stack -> feature-map grid ->
theoretical RF table (stride-1 vs strided/pooled variant of the same layer
stack) -> manual-backward effective RF heatmap -> theoretical/effective
overlay.
