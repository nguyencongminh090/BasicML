# Instructions — TODO-0041

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-17

**Discussed:** User said "DO TASK-0041". Starting it exceeds the WIP cap (TODO-0011 and TODO-0024 already in-progress) — asked the user via `AskUserQuestion`; approved exceeding the cap. Also asked how to break down the ~14-class scope; user chose one commit/PR per category (Loss, then Optimizer, then Layer, then Activation), all on a single `feature/TODO-0041-nn-optim-parity` branch off `dev`, merged to `dev` once the whole TODO is done.
**Next step:** Branch `feature/TODO-0041-nn-optim-parity` off `dev`. Implement in order: Loss (`AbsoluteLoss`, `BinaryCrossEntropy`), Optimizer (`Nesterov`, `Adagrad`, `Adadelta`, `RMSprop`, `Muon`), Layer (`GlobalAvgPool2D`, `GlobalMaxPool2D`, `BatchNorm1D`, `Dropout`), Activation (`Tanh`, `Identity`, `PReLU`, `ELU`, `SELU`, `Softplus`, `Swish`, `Mish`, `Hardtanh`, `Hardsigmoid`) — each following the existing `nn/`/`optim/`/`ops` patterns (header+impl+Doxygen docs, autograd-graph-only, no manual `backward()`), with Catch2 tests per class (forward + numerical-gradient check), keeping `ctest` green throughout.
