# Instructions — TODO-0009

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-03 01:16

**Discussed:** User asked to "write more Optimizer — MiniBatch, Adam, RMSProp,
... and more". Via AskUserQuestion the user selected the full optimizer scope
(Adam + RMSProp + Adagrad + Adadelta + AdamW + Nesterov), a "Mini-batch
Gradient Descent" note for the MiniBatch item, and yes to a comparison example.

**Decision:** Implemented on `feature/TODO-0009-more-optimizers` (off `dev`):

- `optim/adagrad.py` — accumulated squared-grad denominator, `eps=1e-8`.
- `optim/rmsprop.py` — EMA of squared grads, `rho=0.9`, `eps=1e-8`.
- `optim/adadelta.py` — EMA of squared grads and squared steps, no lr needed
  (`lr=1.0` default), `rho=0.95`, `eps=1e-6`.
- `optim/adam.py` — bias-corrected 1st/2nd moments, `beta1=0.9`, `beta2=0.999`.
- `optim/adamw.py` — Adam with decoupled weight decay (`weight_decay=0.01`).
- `optim/nesterov.py` — NAG, Sutskever look-ahead form
  (`v = mu*v - lr*g`; `p += -mu*v_prev + (1+mu)*v`).
- All follow the `Momentum` shape: per-parameter state list, regularizer grad
  added in, `requires_grad and grad is not None` guard, `zero_grad()` loop.
  Core style — zero comments, zero docstrings.

MiniBatch → not an optimizer; added `basicml/datasets/batching.py`
`iter_minibatches(...)` generator (Google-style docstring, the documented
package edge), exported from `datasets/__init__.py`. The example drives
mini-batch GD with it (one `optimizer.step()` per batch).

New example `examples/train_optimizer_comparison.py` — same MLP/init/data,
`OPTIMIZERS` factory dict, log-scale training-BCE curve per optimizer on
`make_moons`.

**Verification:** `MPLBACKEND=Agg python3.13 examples/train_optimizer_comparison.py`
— all 8 optimizers converge (final BCE 0.042–0.054). `pyrefly check` — no new
errors in the added files (9 pre-existing errors in `ref_code.py` /
`plot_dynamic_*` untouched).

**Next step:** commit via `/make-commit`, merge to `dev`, close TODO.

## 2026-09-03 01:21

**Discussed:** User: "Use Adam for Logistic and Linear? (examples)". Via
AskUserQuestion the user chose to switch both.

**Decision:** `examples/train_linear.py` — `Momentum` → `Adam`, `lr` 0.01 → 0.1,
`EPOCHS` 200 → 400, dropped `MOMENTUM`; converges to `f(x) = 2.0000x + -0.0000`.
`examples/train_logistic.py` — `Momentum` → `Adam`, `lr` 0.07 → 0.1,
`MAX_EPOCHS` 20_000 → 3_000, `LOG_EVERY` 2_000 → 250, dropped `MOMENTUM`;
converges in 538 epochs (loss 0.04998 < 0.05 target). Docstrings ("momentum
SGD" → "Adam") and import blocks re-aligned. `pyrefly check` unchanged (9
pre-existing errors).

**Next step:** unchanged — commit, merge to `dev`, close TODO.

## 2026-09-03 01:34

**Discussed:** User: "Update demo with Adam Optimizer". Via AskUserQuestion the
user chose to move *all* `BasicML/demo/` scripts to Adam (including the
cost-surface trio and the vanishing-gradient demo, both flagged as
non-mechanical), and for the trio: keep the one-cycle *lr* schedule, drop
momentum scheduling, rename the "Momentum Path" trace to "Optimizer Path".

**Decision:**

- Mechanical swap (`Momentum` → `Adam`, drop `MOMENTUM`, retune `LEARN_RATE`):
  `plot_dynamic_decision_boundary.py` (lr 0.01), `plot_layer_transformations.py`
  (lr 0.01), `plot_dynamic_layer_morphing.py` (lr 0.01),
  `plot_dynamic_mlp_graph.py` (lr 0.005, keeps `regularizer=reg`).
- `plot_dynamic_vanishing_gradient.py` — swap to Adam (lr 0.003); added a
  docstring paragraph: Adam rescales the *step* by gradient RMS so the two nets'
  loss/acc curves land closer than under SGD, but the vanishing itself is
  unchanged and still visible in panel 4 (Adam never touches the gradient).
- Cost-surface trio (`plot_dynamic_linear` / `_logistic` / `_3d_logistic`) —
  `OneCycle` reduced to an lr-only schedule (`at()` now returns a `float`,
  dropped the `momentum` field and `MOM_CYCLE`), `optimizer.lr = schedule.at(...)`,
  `Momentum` → `Adam`, "Momentum Path" → "Optimizer Path", module docstrings
  updated. linear lr cycle bumped to `(0.02, 0.20, 0.005)`; logistic/3d kept
  `(0.05, 0.4, 0.01)`.

**Verification:** ran all 8 demos headless (`MPLBACKEND=Agg`) — every one trains
and renders: decision_boundary 98.0% acc, layer_transformations loss 0.0141,
layer_morphing 100% acc, mlp_graph plain/reg 95–96% val acc, vanishing_gradient
both nets train, linear cost 0.0005 (early stop @574), logistic cost 0.036, 3d
cost 0.046. `pyrefly check` — same 9 pre-existing errors, none new.

**Next step:** unchanged — commit, merge to `dev`, close TODO.

## 2026-09-03 01:44

**Discussed:** User tested Adam on `plot_dynamic_vanishing_gradient.py` and
found the Sigmoid+Xavier net reaches ~98.5% val — Adam's per-parameter RMS
rescaling divides out the vanished gradient magnitude, so the demo's
"deep Sigmoid net fails to train" contrast disappears at the loss/accuracy
level. After an explanation, the user decided: revert *only* that demo to
Momentum; all others stay on Adam.

**Decision:** `git checkout -- BasicML/demo/plot_dynamic_vanishing_gradient.py`
— undoes FIX-0020's changes to that one file (import, `LEARN_RATE`/`MOMENTUM`
consts, optimizer line, and the added Adam docstring paragraph). Back to
`Momentum(lr=0.10, momentum=0.9)`.

**Verification:** headless run — Sigmoid+Xavier stalls at loss 0.6545 / 60.2%
val acc while ReLU+He hits loss 0.0 / 98.2%: the textbook contrast is restored.

**Next step:** unchanged — commit, merge to `dev`, close TODO.

---

## 2026-09-03 — follow-up: plot_dynamic_linear.py back to a plain loop (FIX-0022)

User asked to remove the adaptive learning rate from `plot_dynamic_linear.py`
and make it "normal as train_linear". FIX-0020 had left this demo on an
lr-only `OneCycle` schedule plus early-stopping.

**Action:** dropped the `OneCycle` dataclass, the schedule call, early-stopping,
and the `MAX_EPOCHS`/`LR_CYCLE`/`WARMUP_FRAC` config. Now `EPOCHS = 400` /
`LEARN_RATE = 0.1` with a plain `Adam` full-batch loop — identical shape to
`examples/train_linear.py`. Docstrings updated.

**Verification:** `pyrefly check` clean for the file; headless
`train_and_record` run completes 400 epochs, final cost 0.0022.
