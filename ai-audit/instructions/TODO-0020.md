# Instructions — TODO-0020

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-11 08:30

**Discussed:** Follow-up to TODO-0019 (untrained feature-map/receptive-field
demo): use real MNIST, train the CNN, and visualize how feature maps behave
during training. Two decisions needed: (1) how to source MNIST given this
repo has no dataset-download infrastructure and lists only
numpy/pandas/matplotlib as dependencies, (2) animated vs. static snapshot
visualization.
**Decision:** User asked to check whether `sklearn` (already installed for
the `/usr/bin/python3.13` interpreter this repo's `pyrefly.toml` targets)
could fetch real MNIST; confirmed `sklearn.datasets.fetch_openml('mnist_784')`
works (70000 x 28x28, ~30s first download, cached afterward). User approved
declaring `scikit-learn` as a new dependency (documented in root `CLAUDE.md`,
scoped to dataset-fetching only) and chose the animated-over-training
visualization style, matching this repo's `plot_dynamic_*.py` demos.
**Next step:** none — implemented, verified, and closed in this session.

## 2026-09-11 09:40

**Discussed:** User asked for the demo to use digit "8" as the example
(was tracking whatever the first shuffled training sample happened to be —
digit "0" in earlier test runs).
**Decision:** Added `TARGET_DIGIT = 8` config constant and a
`pick_sample(X, y_int, digit)` helper (first matching-label training image,
raises `ValueError` if the subset has none of that digit) to
`plot_dynamic_cnn_feature_maps.py`; `main()` now calls it instead of always
taking index 0.
**Next step:** none — verified the helper locates an actual "8" sample and
both demos still run end to end.
