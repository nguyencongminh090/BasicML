# Instructions — TODO-0033

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-13

**Discussed:** Split off from TODO-0032 in the same session (see `instructions/TODO-0032.md`) — `Loss`/`Optimizer` abstractions weren't attempted in TODO-0032's first slice. Starting it immediately puts 4 TODOs `in-progress` at once (TODO-0011, TODO-0024, TODO-0032, TODO-0033), one beyond the exception already granted for TODO-0032.
**Decision:** User extended the same WIP-cap exception to cover TODO-0033 too (asked via AskUserQuestion, chose "extend exception" over waiting). Branching directly off the current uncommitted `feature/TODO-0032-module-api` work rather than a fresh branch off `dev`, since TODO-0032's work isn't committed yet — branch/commit split to be sorted out at commit time (user asked not to commit yet, to conserve session quota).
**Next step:** Implement `Loss` base + `MSELoss`, `Optimizer` base + `SGD` refactor under `AdvanceML/include/advanceml/nn/loss.hpp` and `AdvanceML/include/advanceml/optim/optimizer.hpp`/`sgd.hpp`, per the TODO's requirement doc.

Implemented and verified same session: `Loss` (abstract `operator()(pred, target) -> Tensor`, no separate `backward()` needed) + `MSELoss` wrapping `mse_loss`; `Optimizer` (abstract `step()`/`zero_grad()`) + `SGD` moved under `optim/` deriving from it, behavior unchanged. Old flat `advanceml/optim.hpp`/`src/optim.cpp` deleted. `nn_test.cpp`'s XOR test now uses `MSELoss`/`SGD` via the new headers — the full `Sequential`/`Linear`/`ReLU`/`MSELoss`/`SGD` proof the original TODO-0032 requirement asked for. `cmake --build` + `ctest` pass 5/5, including the pre-existing `SGD` test with unmodified behavior. See FIX-0054. Not committed yet.
