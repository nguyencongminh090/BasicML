# Instructions — TODO-0023

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-11 10:40

**Discussed:** Follow-up to TODO-0020/0021/0022 (custom CNN / LeNet-shaped /
AlexNet-shaped trained feature-map demos): "design efficient architecture...
or try modern optimizer" to push MNIST accuracy past ~99%. Flagged first
that the ~99% numbers so far are *train* accuracy on 1200-3000-image
subsets with no held-out test set — not a trustworthy number regardless of
architecture/optimizer choice. Two questions asked: (1) whether to also fix
that methodology (larger training set + real test split), (2) whether to
apply architecture improvements, optimizer improvement, or both.
**Decision:** User chose both: fix the eval methodology (larger train set +
genuine held-out test split, report test accuracy) AND apply both
improvements (efficient architecture using `BatchNorm2D`/`GlobalAvgPool2D`,
neither used by any demo so far; and `AdamW`, also unused so far — both
already existed in the library except `BatchNorm2D`, which needed adding
since the existing `BatchNorm` is 2D-only).
**Next step:** none — implemented as `basicml/nn/batchnorm.py::BatchNorm2D`
+ `BasicML/examples/train_cnn_mnist.py`, gradient-checked, verified, and
closed in this session.
