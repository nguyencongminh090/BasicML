# Instructions — TODO-0012

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-05 (session)

**Discussed:** User asked (in Vietnamese) for a 2-feature (x1, x2) dataset
distributed "hình parabol" (parabola-shaped), visualized like the existing
`plot_dynamic_linear.py` demo. Ambiguous on three axes, so asked via
AskUserQuestion before implementing: (1) regression vs classification task,
(2) whether "parabola" means x2 correlated to x1 via x1^2, or points
scattered around a parabola curve in the (x1,x2) plane, (3) how the 5-panel
visualization should adapt now that the weight is a 2D vector instead of a
scalar.

**Decision:** Linear regression; points scatter around a parabola curve in
the (x1, x2) plane (`x2 = curvature * x1**2 + noise`); keep the 5-panel
layout but re-parameterize the contour/surface/cost-vs-weight panels over
`(w1, w2)` instead of `(w, b)` (bias held fixed at its closed-form optimum).
Panel 1 (fit) was not explicitly covered by the question — since a single
2D fit line has no equivalent with 2 input features, adapted it to a 3D
scatter of (x1, x2, y) with the fitted plane redrawn each frame, keeping
the same "epoch title + evolving fit" spirit as the original.

**Next step:** None — implemented and verified in this session (see
FIX-0030). TODO closed as done.

## 2026-09-05 13:07 (follow-up)

**Discussed:** User pointed out the static gray reference curve in panel 5
("Cost vs Weight (w1)") — the analytic cost-vs-w1 parabola with w2/b fixed
at their closed-form optimum — added no value since it wasn't the trained
trajectory, just a reference line.

**Decision:** Removed the gray curve and the now-unused `cost_vs_w1()`
helper entirely; panel 5 now only shows the purple optimizer trajectory
(cost at the actual `w1` per epoch), with the y-axis scaled from
`history.cost.max()` instead of the removed reference curve's max.

**Next step:** None — re-verified with a headless render (see FIX-0031).
