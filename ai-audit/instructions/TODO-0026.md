# Instructions — TODO-0026

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-11 11:35

**Discussed:** "Try improve LeNet with Muon or enhance LeNet. I think LeNet
is good for MNIST?" -- follow-up to TODO-0025's cancelled double-conv
comparison. Confirmed LeNet is indeed well-suited to MNIST (designed for
handwritten digit recognition, appropriately scaled for MNIST's
simplicity), and chose to retry the AdamW-vs-Muon comparison on LeNet
specifically because it's much cheaper than the double-conv net that made
TODO-0025 impractical.
**Decision:** Built `examples/train_lenet_mnist.py` -- LeNet's existing
6/16-channel, 120/84-FC shape, with a real train/test split added (the
existing `demo/plot_dynamic_lenet_feature_maps.py` only reports train
accuracy on a 1200-image subset), same Muon(weights)/AdamW(biases)
parameter-split methodology as TODO-0025. Smoke-tested first this time
(learned from TODO-0025's wrong runtime estimate): ~6 min AdamW / ~18 min
Muon for the full 15000-train/15-epoch run -- Muon costs more per step
(Newton-Schulz iteration) but showed much faster early convergence in the
smoke test (87% test acc after 1 epoch vs. AdamW's 60%). Both launched as
parallel background tasks.
**Next step:** waiting on both background runs (`bg1cif4os` AdamW,
`b6ehkmm9d` Muon+AdamW) to finish, then compare final test accuracy and
close this TODO.

## 2026-09-11 11:55

**Discussed:** Both runs completed. AdamW: stable, best test accuracy
98.40% (epoch 10), ends 98.20% (epoch 15) -- confirms LeNet is genuinely
well-suited to MNIST at real held-out accuracy, not just train accuracy.
Muon+AdamW: started strong (95.60% after epoch 1, ahead of AdamW's 94.40%)
and stayed competitive through epoch ~7, but then diverged -- test loss
climbs from ~0.15 to 1.31, test accuracy collapses to 60.40% at epoch 14
before a partial bounce to 73.00% at epoch 15. Best Muon epoch was 96.80%
(epoch 5), still below AdamW's peak.
**Decision:** Reported the result as-is rather than re-tuning and re-running
-- `MUON_LR=0.02` was carried over unchanged from `train_optimizer_comparison.py`'s
tiny 32-hidden-unit MLP problem; the divergence pattern (stable early,
diverging late, no LR decay in this script) is the classic signature of a
learning rate that's too high for this parameter scale, not evidence Muon
is unsuited to CNNs. Flagged the likely fix (lower `MUON_LR` and/or add a
decay schedule) rather than silently trying more configurations unprompted.
**Next step:** none unless the user wants a re-tuned Muon run.


**WIP-limit note:** same as TODO-0025 -- opening this makes a 3rd concurrent
in-progress TODO alongside TODO-0011 and TODO-0024, both owned by other
sessions working in this repo. Flagging rather than unilaterally parking
either; expected to close quickly once the background runs finish.
