# Instructions — TODO-0028

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-12 07:56

**Discussed:** "Write again demo with lenet use AdamW optimizer. You can
divide -> main file & tuning (hyper tuning) files and connect via a
config." Read the existing `train_lenet_mnist.py` (AdamW vs. Muon
comparison, TODO-0026/0027) and `train_cnn_mnist.py` as the pattern to
follow for a plain AdamW-only rewrite, then split it three ways per the
user's instruction.
**Decision:** Kept the exact LeNet-5-shaped architecture (6/16 conv
channels, 120/84 FC) from `train_lenet_mnist.py`, dropped Muon entirely
(AdamW already won that comparison decisively), and introduced
`lenet_config.LeNetConfig` (frozen dataclass) as the shared config both
`train_lenet_adamw.py` (single training run) and `tune_lenet_adamw.py`
(hyperparameter grid search) build on via `dataclasses.replace()` — no
hyperparameter is duplicated as a second literal anywhere. `pyrefly check`
initially failed on the cross-file imports (`from lenet_config import
...`) since pyrefly's search root is `BasicML/`, not `BasicML/examples/`;
fixed by importing as `examples.lenet_config` / `examples.train_lenet_adamw`
(this resolves both statically and at runtime, since `sys.path` already
has the `BasicML/` directory appended and `examples/` needs no
`__init__.py` as a Python 3 namespace package).
**Verification:** `pyrefly check` clean for all three new files. Smoke-
tested `train_lenet_adamw.run()` directly with `n_train=300, n_test=100,
epochs=2` (ran cleanly, accuracy rose epoch to epoch as expected).
Smoke-tested `tune_lenet_adamw.main()` with a monkey-patched tiny grid (2
lr candidates, 1 weight_decay, 1 epoch, 200/80 train/test images, and a
patched `run()` to keep the final-validation step just as cheap) — ranking
printed correctly, best candidate selected, final-validation run executed
and reported a held-out accuracy. Neither the real 9-candidate search nor
a full-budget (15000/2500, 15-epoch) run was executed in this session —
that's left for the user to run when they want actual numbers, since it
takes real wall-clock time (rough estimate: 9 cheap-budget trials at
~4 epochs/4000 train images each, plus one full validation run).
**Next step:** none unless the user asks to actually run the search and
report real numbers.
