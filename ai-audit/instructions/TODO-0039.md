# Instructions — TODO-0039

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-17

**Discussed:** User said "DO TASK 0034" (the AdvanceML umbrella, not directly actionable). Asked which remaining open area to start — options were area 5 (datasets/metrics) and area 6 (MNIST example, which depends on area 5). User chose area 5.
**Decision:** File TODO-0039 covering area 5: a C++ MNIST IDX-format loader and an accuracy metric helper. Starting a new sub-TODO exceeds the WIP cap again (TODO-0011 and TODO-0024 still both in-progress) — asked the user via `AskUserQuestion`, who approved the exception, same as TODO-0035/0036/0037/0038.
**Next step:** Branch `feature/TODO-0039-datasets-metrics` off `dev`, implement the IDX loader + accuracy metric with Doxygen docs and Catch2 gradient/unit tests, keep the build/test suite green, then commit via `commit-task` and file a fix-log entry.
