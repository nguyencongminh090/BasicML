# Instructions — TODO-0003

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-02 — demo authored (AI-implemented)

**Discussed:**
- User confirmed the mental model: regularization exists to fight overfitting / reduce
  effective model complexity — correct. Framed against bias–variance: high variance =
  large gap between train and (dev/val) performance; regularization trades a little train
  fit for a smaller gap.
- Whether to wait for a dedicated model-diagnostic helper. Decided not to block: the demo
  computes its own train/val accuracy + gap inline. A metrics/helper module is a later
  upgrade (own TODO).
- Scope choice (AskUserQuestion): user picked **self-contained example first**, revisit for
  shared metrics helpers later.

**Decision:**
- `BasicML/examples/train_regularization.py`: `make_moons` (small noisy train split, larger
  clean-ish val set), over-capacity MLP `Linear(2,64)-ReLU-Linear(64,64)-ReLU-Linear(64,1)-Sigmoid`
  (`he` init), `BinaryCrossEntropy`, `Momentum`. Three runs sharing seed/data: no reg,
  `L2(lambda_)`, `L1(lambda_)`. Report train/val loss + accuracy + gap; decision-boundary
  plot behind `SHOW_PLOT`.
- Reported loss is data-only; `reg.penalty()` shown separately as the R(theta) term.

**Next step:**
- User runs `pyrefly check` + the script; commit via `/make-commit` when satisfied.
- Later TODO: extract `accuracy` / `train_val_split` into a shared `basicml` metrics module
  and upgrade this demo to use it.

## 2026-09-02 17:22 — improve pass (AI-implemented, FIX-0003)

**Discussed:**
- User had edited `build_model` to use `Sigmoid` in the hidden layers; the `L2(0.05)` run
  then collapsed to the trivial 0.5 predictor (BCE = ln 2, acc 0.5, R = 0). Explained cause:
  saturating sigmoid hidden layers shrink the data gradient >=4x/layer, so the constant
  `lambda_ * param` weight-decay term dominates and drives all weights to a dead equilibrium.
  Confirmed with a lambda sweep (0.01/0.005 recover; 0.05 dead). User asked for a fix.

**Decision:**
- Restore `ReLU` hidden activations (`he` init) so `LAMBDA_L2 = 0.05` behaves; added a module
  docstring paragraph explaining why hidden layers must not be Sigmoid here.
- Added `History` (per-epoch train/val BCE) + `Result` dataclass-style holder; new
  `print_summary` table and a 2xN plot (decision boundary on top, learning curve below —
  val curve separating upward = the visual overfit signal).
- `N_VAL` 200 -> 800 (steadier val estimate).

**Verification:** ran under `MPLBACKEND=Agg`, exit 0. Final: no-reg train/val acc 1.000/0.794
(loss gap +2.99); L2 0.875/0.845 (+0.009); L1 0.900/0.843 (+0.155). `pyrefly` still not on
PATH — user to run.

**Next step:** unchanged — user runs pyrefly + commits; shared-metrics TODO still pending.

## 2026-09-02 17:29 — English + val plot (AI-implemented, FIX-0004)

**Discussed:** User asked for (1) all code/comments/docstring in English (this file only —
the repo default is Vietnamese docs per CLAUDE.md, but the user explicitly overrode for
this example), (2) a plot showing model behaviour on the val set.
**Decision:** Translated the whole file to English. Plot is now 3xN: row 1 boundary over
train, row 2 same model over the held-out val set with `x` markers on misclassified val
points (`mark_wrong=True`), row 3 learning curves. Factored `_boundary_grid` helper.
**Verification:** ran under `MPLBACKEND=Agg`, exit 0.
**Next step:** unchanged.
