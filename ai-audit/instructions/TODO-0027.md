# Instructions — TODO-0027

Session-by-session solution notes for this TODO. Append a new dated section each time a chat session makes progress on it — never delete or rewrite prior entries.

## 2026-09-11 12:05

**Discussed:** "Apply learning-rate-decay for muon" -- direct fix for
TODO-0026's diagnosed cause of Muon's divergence (fixed `lr=0.02`, no decay,
carried over unchanged from a much smaller MLP problem).
**Decision:** Added a cosine decay (`base_lr` -> `base_lr * 0.05` over the
run) applied only to Muon's `lr`, via a `decay` parameter on `train()`
rather than a global scheduler utility in `basicml.optim` -- this repo has
no existing LR-scheduler abstraction (only one ad hoc `OneCycle` dataclass
inside a single demo file), and a training-loop-local helper matches that
existing convention rather than introducing new core-library API for a
one-off experiment. Only Muon's `lr` decays; the AdamW-only baseline and the
Muon run's auxiliary `AdamW` keep fixed `lr`, since the divergence was
isolated to Muon's own updates specifically.
**Next step:** waiting on the re-run (`bvcn1tulq`) to finish, then compare
against TODO-0026's AdamW (98.40% best) and non-decayed Muon (96.80% best,
diverged to 73.00%) results.

## 2026-09-11 12:10

**Discussed:** User said "stop" while the decayed-Muon re-run was in
flight.
**Decision:** Stopped background task `bvcn1tulq` via `TaskStop`. The code
change itself (`cosine_decay_lr()` + `train()`'s `decay` parameter) is kept
in `examples/train_lenet_mnist.py` -- verified correct via the smoke test
(0.02 -> 0.001 decay curve, training ran cleanly) -- only the long
background run was abandoned, not the deliverable.
**Next step:** none unless the user asks to re-run and get the actual
decayed-Muon accuracy numbers later.

**WIP-limit note:** same recurring situation as TODO-0025/0026 -- opening
this makes a 3rd concurrent in-progress TODO alongside TODO-0011 and
TODO-0024, both owned by other sessions. Flagging rather than unilaterally
parking either.
